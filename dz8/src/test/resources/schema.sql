DROP TABLE IF EXISTS AUTHORS;
CREATE TABLE AUTHORS(ID INT AUTO_INCREMENT(1,1) PRIMARY KEY, NAME VARCHAR(255));
DROP TABLE IF EXISTS GENRES;
CREATE TABLE GENRES(ID INT AUTO_INCREMENT(1,1) PRIMARY KEY, NAME VARCHAR(255));
DROP TABLE IF EXISTS BOOKS;
CREATE TABLE BOOKS(ID INT AUTO_INCREMENT(1,1) PRIMARY KEY, NAME VARCHAR(255), AUTHOR_ID INT, GENRE_ID INT, FOREIGN KEY (AUTHOR_ID) references AUTHORS(ID), FOREIGN KEY (GENRE_ID) references GENRES(ID));